/*
import { COMMENTS } from '../shared/comments';
import { PROMOTIONS } from '../shared/promotions';
import { LEADERS } from '../shared/leaders';  


export const initialState ={
    dishes: DISHES,
    comments: COMMENTS,
    promotions: PROMOTIONS,
    leaders: LEADERS
}

export const Reducer = (state = initialState,action) =>{  //when the reducer is first called by my store, my store would have no state, so the state will be uninitialized
                                                          // So it will be undefined there. So, in order to avoid issues with that 
                                                          //I'm going to say that if the state is undefined, the default value will be initialState (ES6)
    return state
};
*/


// this file is not needed since we already created 4 reducer functions